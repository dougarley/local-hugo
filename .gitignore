# Hugo build output
public/
.hugo_build.lock

# Tailwind standalone binary
tailwindcss

# Generated CSS (rebuilt from input.css)
assets/css/output.css
assets/css/daisyui.css

# macOS
.DS_Store

# Editor
*.code-workspace
.vscode/

# Local config — contains sensitive NAS credentials, never commit
.claude-local.md

# Local dev launcher — contains API keys, never commit
dev.ps1

# npm
node_modules/

# Claude Code local settings (contains NAS password in allowed commands)
.claude/settings.local.json
